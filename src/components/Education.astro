<div class="page">

  <!-- COL 1 — Vzdelanie -->
  <div class="col col-uni">
    <div class="section-head">
      <h2 class="section-title">Vzdelanie</h2>
    </div>

    <div class="edu-list">

      <div class="edu-row edu-row--current">
        <div class="edu-left">
          <span class="edu-dot edu-dot--current"></span>
          <span class="edu-period">2024 – súčasnosť</span>
        </div>
        <div class="edu-right">
          <div class="edu-header">
            <h3 class="edu-degree">Ing. Inžinierské štúdium</h3>
            <span class="edu-badge">Prebieha</span>
          </div>
          <p class="edu-field">Informačný manažment</p>
          <p class="edu-school">Fakulta riadenia a informatiky, Žilinská univerzita</p>
        </div>
      </div>

      <div class="edu-row">
        <div class="edu-left">
          <span class="edu-dot"></span>
          <span class="edu-period">2021 – 2024</span>
        </div>
        <div class="edu-right">
          <div class="edu-header">
            <h3 class="edu-degree">Bc. Bakalárske štúdium</h3>
          </div>
          <p class="edu-field">Informatika a riadenie</p>
          <p class="edu-school">Fakulta riadenia a informatiky, Žilinská univerzita</p>
          <p class="edu-note">Téma BP ->  Dátová analytika vo vzdelávacom procese</p>
        </div>
      </div>

      <div class="edu-row">
        <div class="edu-left">
          <span class="edu-dot"></span>
          <span class="edu-period">2017 – 2021</span>
        </div>
        <div class="edu-right">
          <div class="edu-header">
            <h3 class="edu-degree">Gymnázium Šrobárova 1</h3>
          </div>
          <p class="edu-school">Košice, SR</p>
        </div>
      </div>

    </div>
  </div>

  <!-- COL 2 — Erasmus+ -->
  <div class="col col-erasmus">
    <div class="section-head">
      <h2 class="section-title">Erasmus+</h2>
    </div>

    <div class="era-list">

      <div class="era-entry">
        <div class="era-head">
          <div>
            <h3 class="era-country">Chorvátsko — Šibenik</h3>
            <p class="era-uni">Veleučilište u Šibeniku</p>
          </div>
          <div class="era-aside">
            <span class="era-period">mar – júl 2026</span>
          </div>
        </div>
        <div class="era-tags">
          <span class="era-tag">Business Information Systems</span>
          <span class="era-tag">Databázové systémy</span>
          <span class="era-tag">Dátová analýza (R)</span>
          <span class="era-tag">Google automatizácia</span>
        </div>
        <p class="era-takeaway">Zameranie na informačné systémy, dátovú analytiku a databázy doplnené o tímové projekty s medzinárodnými študentmi.</p>
      </div>

      <div class="era-entry">
        <div class="era-head">
          <div>
            <h3 class="era-country">Fínsko — Vantaa</h3>
            <p class="era-uni">Laurea University of Applied Sciences</p>
          </div>
          <div class="era-aside">
            <span class="era-period">jan – máj 2024</span>
          </div>
        </div>
        <div class="era-tags">
          <span class="era-tag">Softvérové testovaie</span>
          <span class="era-tag">Data-Driven Decision Making</span>
          <span class="era-tag">Digitálny marketing</span>
        </div>
        <p class="era-takeaway">Projektovo orientovaná výučba zameraná na softvérové testovanie, rozhodovanie na základe dát a spoluprácu v medzinárodných tímoch.</p>
      </div>

      <div class="bip-note">
        <span class="bip-label">BIP</span>
        <div class="bip-content">
          <p class="bip-where">České Budějovice</p>
          <p class="bip-what">Vysoká škola technická a ekonomická v Českých Budějovicích</p>
          <p class="bip-what">Rozvoj soft skills v medzinárodnom prostredí</p>
        </div>
      </div>

    </div>
  </div>

   <!--COL 3 — Kurzy + Certifikáty--> 
  <div class="col col-right">

    <!--<div class="right-block">
      <div class="section-head">
        <h2 class="section-title">Kurzy</h2>
      </div>
      <ul class="course-list">
        <li class="course-item">
          <span class="course-name">Python</span>
        </li>
        <li class="course-item">
          <span class="course-name">SQL</span>
        </li>
        <li class="course-item">
          <span class="course-name">Data Analytics</span>
        </li>
        <li class="course-item">
          <span class="course-name">Testing</span>
        </li>
        <li class="course-item">
          <span class="course-name">Git</span>
        </li>
      </ul>
    </div>-->

    <div class="right-block">
      <div class="section-head">
        <h2 class="section-title">Certifikáty</h2>
      </div>
      <!-- Príklad certifikátu s Credly linkom:-->
      <ul class="cert-list">
        <li class="cert-item">
          <div class="cert-header">
            <span class="cert-name">ADONIS Process Management</span>
            <span class="cert-date">2025</span>
          </div>
          <p class="cert-org">UNIZA & BOC Group</p>
          <p class="cert-desc">Certifikácia zameraná na procesné riadenie v nástroji ADONIS so zameraním na modelovanie procesov v BPMN, 
          analýzu, dokumentáciu a optimalizáciu podnikových procesov.</p>
        </li>
        <li class="cert-item">
          <div class="cert-header">
            <span class="cert-name">Google AI Essentials V1</span>
            <span class="cert-date">2025</span>
          </div>
          <p class="cert-org">Coursera</p>
          <p class="cert-desc">Držiteľ certifikátu preukázal znalosti v oblasti umelej inteligencie a schopnosť efektívne
           využívať generatívnu AI pri pracovných úlohách, vrátane zodpovedného používania AI.</p>
          <a href="https://www.credly.com/earner/earned/share/d2b0a9f0-952f-4e73-8b76-9a46deeaf21c" target="_blank" rel="noopener" class="cert-credly">
            Zobraziť na Credly ↗
          </a>
        </li>
      </ul>
      
    </div>

  </div>
</div>

<style>
  /* ── Three-column no-scroll layout ── */
  .page {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    height: 100%;
    overflow: hidden;
  }

  .col {
    overflow-y: auto;
    padding: 2.5rem;
    border-right: 1px solid var(--border-muted);
  }
  .col:last-child { border-right: none; }

  .col-right {
    display: flex;
    flex-direction: column;
    padding: 0;
    overflow-y: auto;
  }
  .right-block {
    padding: 2.5rem;
    border-bottom: 1px solid var(--border-muted);
    flex: 1;
  }
  .right-block:last-child { border-bottom: none; }

  /* ── Section headings ── */
  .section-head {
    display: flex;
    align-items: baseline;
    gap: 1rem;
    margin-bottom: 2rem;
    padding-bottom: 1rem;
    border-bottom: 1px solid var(--border-muted);
  }
  .section-title {
    font-family: var(--font-display);
    font-size: 1.35rem;
    font-weight: 700;
    color: var(--text-primary);
  }

  /* ── University rows ── */
  .edu-list { display: flex; flex-direction: column; }

  .edu-row {
    display: grid;
    grid-template-columns: 48px 1fr;
    gap: 1rem;
    padding: 1.2rem 0;
    border-bottom: 1px solid var(--border-muted);
    align-items: start;
  }
  .edu-row:last-child { border-bottom: none; }

  .edu-left {
    display: flex;
    flex-direction: column;
    gap: 0.45rem;
    padding-top: 0.15rem;
  }
  .edu-dot {
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--border);
    display: block;
    flex-shrink: 0;
  }
  .edu-dot--current { background: var(--orange); box-shadow: 0 0 8px rgba(240,136,62,0.4); }
  .edu-period {
    font-size: 0.68rem;
    color: var(--text-muted);
    line-height: 1.4;
  }

  .edu-right { flex: 1; }
  .edu-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: 0.2rem;
  }
  .edu-degree {
    font-size: 0.87rem;
    font-weight: 600;
    color: var(--text-primary);
  }
  .edu-badge {
    font-size: 0.62rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: var(--orange);
    background: rgba(240,136,62,0.1);
    border: 1px solid rgba(240,136,62,0.25);
    padding: 0.15rem 0.5rem;
    border-radius: 4px;
    white-space: nowrap;
    flex-shrink: 0;
  }
  .edu-field {
    font-size: 0.78rem;
    color: var(--text-secondary);
    margin-bottom: 0.15rem;
  }
  .edu-school {
    font-size: 0.72rem;
    color: var(--text-muted);
    margin-bottom: 0.35rem;
  }
  .edu-note {
    font-size: 0.7rem;
    background: var(--bg-elevated);
    border: 1px solid var(--border-muted);
    padding: 0.2rem 0.55rem;
    border-radius: 4px;
    display: inline-block;
    margin-top: 0.2rem;
    line-height: 1.45;
  }

  /* ── Erasmus ── */
  .era-list {
    display: flex;
    flex-direction: column;
  }

  .era-entry {
    position: relative;
    padding: 0.2rem 0 1.5rem 1.25rem;
    border-left: 2px solid var(--border-muted);
  }
  .era-entry--current { border-left-color: var(--orange); }

  .era-entry::before {
    content: '';
    position: absolute;
    left: -5px; top: 0.3rem;
    width: 8px; height: 8px;
    border-radius: 50%;
    background: var(--border);
    border: 2px solid var(--bg);
    box-sizing: border-box;
  }
  .era-entry--current::before {
    background: var(--orange);
    box-shadow: 0 0 8px rgba(240,136,62,0.4);
  }

  .era-head {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    gap: 0.75rem;
    margin-bottom: 0.6rem;
  }
  .era-country {
    font-size: 0.88rem;
    font-weight: 600;
    color: var(--text-primary);
    margin-bottom: 0.15rem;
  }
  .era-uni {
    font-size: 0.7rem;
    color: var(--text-muted);
    line-height: 1.4;
  }
  .era-aside {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.3rem;
    flex-shrink: 0;
  }
  .era-badge {
    font-size: 0.6rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: var(--orange);
    background: rgba(240,136,62,0.1);
    border: 1px solid rgba(240,136,62,0.25);
    padding: 0.12rem 0.45rem;
    border-radius: 4px;
  }
  .era-period {
    font-size: 0.65rem;
    color: var(--text-muted);
    white-space: nowrap;
  }

  .era-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
    margin-bottom: 0.6rem;
  }
  .era-tag {
    display: inline-block;
    font-size: 0.62rem;
    color: var(--blue);
    background: var(--blue-dim);
    border: 1px solid rgba(88,166,255,0.2);
    padding: 0.12rem 0.45rem;
    border-radius: 4px;
    line-height: 1.5;
  }

  .era-takeaway {
    font-size: 0.72rem;
    color: var(--text-muted);
    line-height: 1.5;
    font-style: italic;
    margin: 0;
  }

  /* ── BIP ── */
  .bip-note {
    display: flex;
    align-items: flex-start;
    gap: 0.65rem;
    padding: 0.65rem 0.85rem;
    border: 1px dashed var(--border-muted);
    border-radius: 6px;
    margin-top: 0.5rem;
  }
  .bip-label {
    font-size: 0.55rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    background: var(--bg-elevated);
    border: 1px solid var(--border-muted);
    padding: 0.12rem 0.4rem;
    border-radius: 3px;
    white-space: nowrap;
    flex-shrink: 0;
    margin-top: 0.1rem;
  }
  .bip-content {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }
  .bip-where {
    font-size: 0.72rem;
    font-weight: 600;
    color: var(--text-secondary);
    margin: 0;
  }
  .bip-what {
    font-size: 0.68rem;
    color: var(--text-muted);
    line-height: 1.45;
    margin: 0;
  }

  /* ── Courses ── */
  .course-list {
    list-style: none;
    display: flex;
    flex-direction: column;
  }
  .course-item {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
    padding: 0.7rem 0;
    border-bottom: 1px solid var(--border-muted);
  }
  .course-item:last-child { border-bottom: none; }
  .course-name {
    font-size: 0.83rem;
    font-weight: 600;
    color: var(--text-primary);
  }
  .course-desc {
    font-size: 0.72rem;
    color: var(--text-muted);
    line-height: 1.45;
  }

  /* ── Certificates ── */
  .cert-list {
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }
  .cert-item {
    padding: 0.85rem 1rem;
    border: 1px solid var(--border-muted);
    border-radius: 6px;
    background: var(--bg-elevated);
  }
  .cert-header {
    display: flex;
    justify-content: space-between;
    align-items: baseline;
    margin-bottom: 0.15rem;
  }
  .cert-name {
    font-size: 0.83rem;
    font-weight: 600;
    color: var(--text-primary);
  }
  .cert-date {
    font-size: 0.68rem;
    color: var(--text-muted);
  }
  .cert-org {
    font-size: 0.72rem;
    color: var(--text-secondary);
    margin-bottom: 0.2rem;
  }
  .cert-desc {
    font-size: 0.7rem;
    color: var(--text-muted);
    line-height: 1.45;
    margin-bottom: 0.5rem;
  }
  .cert-credly {
    display: inline-flex;
    align-items: center;
    gap: 0.25rem;
    font-size: 0.68rem;
    font-weight: 500;
    color: var(--blue);
    text-decoration: none;
    transition: opacity 0.15s;
  }
  .cert-credly:hover { opacity: 0.75; }

  /* ── Empty state ── */
  .empty-note {
    font-size: 0.72rem;
    color: var(--text-muted);
    font-style: italic;
  }

  /* ── Mobile ── */
  @media (max-width: 900px) {
    .page {
      grid-template-columns: 1fr;
      height: auto;
      overflow: visible;
    }
    .col {
      border-right: none;
      border-bottom: 1px solid var(--border-muted);
      overflow-y: visible;
    }
    .col-right {
      padding: 0;
      overflow-y: visible;
    }
    .col:last-child { border-bottom: none; }
    .edu-row { grid-template-columns: 1fr; gap: 0.5rem; }
    .edu-left { flex-direction: row; align-items: center; gap: 0.75rem; }
  }
</style>